



# Updating the config
aws eks --region <region> update-kubeconfig --name <cluster-name> --kubeconfig /tmp/kube/config

# Specifying the config Environment Variable
export KUBECONFIG=/tmp/kube/config

#use
$kubectl get pods
